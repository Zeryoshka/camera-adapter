[Unit]
Description="Upravlyator for cameras"
After=multi-user.target

[Service]
User=root
Group=root
Type=simple
Restart=always
RestartSec=1
ExecStart={{ upravlyator_path }} --conf {{ project_root }}/conf.yaml

[Install]
WantedBy=multi-user.target
